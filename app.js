let sortMode='new';
function renderAll(){renderFolders();renderGallery()}
function renderGallery(){const gallery=document.getElementById('gallery');let images=getVisibleImages();images.sort((a,b)=>sortMode==='new'?new Date(b.date)-new Date(a.date):new Date(a.date)-new Date(b.date));const folderTitle=state.currentFolder.replace('Thumbnails/','');document.getElementById('pageTitle').textContent=state.currentFolder.startsWith('Thumbnails/')&&folderTitle!==state.currentFolder?'Thumbnails · '+folderTitle:state.currentFolder;document.getElementById('status').textContent=images.length+' image'+(images.length===1?'':'s');if(!images.length){gallery.innerHTML='<div class="empty"><strong>No images here</strong>Add graphics to this section.</div>';return}gallery.innerHTML=images.map(x=>`<article class="card"><img class="thumb" src="${x.data}" alt="" data-view="${x.id}"><div class="card-body"><div class="card-title" title="${escapeHtml(x.name)}">${escapeHtml(x.name)}</div><div class="meta">Uploaded ${new Date(x.date).toLocaleString()}</div><div class="tags">${(x.tags||[]).map(t=>`<span class="tag">${escapeHtml(t)}</span>`).join('')}</div><div class="card-actions"><button data-view="${x.id}">View</button><button class="delete" data-delete="${x.id}">Delete</button></div></div></article>`).join('');gallery.querySelectorAll('[data-view]').forEach(el=>{el.onclick=()=>{const x=state.images.find(i=>i.id===el.dataset.view);if(x)showViewer(x.data)}});gallery.querySelectorAll('[data-delete]').forEach(el=>{el.onclick=()=>{if(confirm('Delete this image?')){state.images=state.images.filter(i=>i.id!==el.dataset.delete);saveState();renderAll()}}})}
document.getElementById('imageInput').onchange=e=>handleUpload([...e.target.files]);
document.getElementById('searchInput').oninput=renderGallery;
document.getElementById('newest').onclick=()=>{sortMode='new';renderGallery()};
document.getElementById('oldest').onclick=()=>{sortMode='old';renderGallery()};
loadState();renderAll();
