function fileToDataURL(file){return new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(r.result);r.onerror=reject;r.readAsDataURL(file)})}
async function handleUpload(files){for(const file of files){if(!file.type.startsWith('image/'))continue;const item={id:Date.now()+'_'+Math.random().toString(36).slice(2),name:file.name,folder:state.currentFolder,data:await fileToDataURL(file),text:'',tags:[],date:new Date().toISOString(),scanned:false};state.images.unshift(item);saveState();renderAll();scanImage(item)}document.getElementById('imageInput').value=''}
